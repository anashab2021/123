pipeline {
    agent any
    stages {
        stage('Install the git') {
            steps {
                sshagent(['ssh']) {
		sh 'su ubuntu'
		sh 'cd /home/ubuntu/123 && git pull https://github.com/anashab2021/123.git'
		sh 'docker-compose -f /home/ubuntu/123/docker-compose.yml up -d'

            }
        }
    }
        stage('Run docker compose') {
            steps {
                sshagent(['ssh']) {
		sh 'docker-compose -f /home/ubuntu/123/docker-compose.yml up -d'
            }
        }
	}
    }
}
